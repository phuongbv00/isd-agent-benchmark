[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "isd-evaluator"
version = "0.1.0"
description = "교수설계 Agent 평가 시스템 - CIDPP 메트릭 기반"
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
authors = [
    { name = "ISD Agent Team" }
]
keywords = ["instructional-design", "evaluation", "addie", "cidpp", "benchmark"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Education",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]

dependencies = [
    "openai>=1.0.0",
    "langchain>=0.3.0",
    "langchain-openai>=0.2.0",
    "pydantic>=2.0.0",
    "typer>=0.12.0",
    "rich>=13.0.0",
    "python-dotenv>=1.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "ruff>=0.4.0",
]
# Neural text encoder for the alignment metric (metrics/alignment.py).
# The default TF-IDF char-ngram fallback needs nothing beyond the stdlib.
alignment = [
    "sentence-transformers>=2.5.0",
]
# Fine-tuning + local inference of the transformer Bloom classifier
# (scripts/train_bloom_6way_classifier.py, TransformerBloomClassifier).
bloom-train = [
    "transformers>=4.40.0",
    "accelerate>=0.30.0",
    "torch>=2.2.0",
    "scikit-learn>=1.4.0",
]

[project.scripts]
isd-evaluator = "isd_evaluator.cli:app"

[tool.hatch.build.targets.wheel]
packages = ["src/isd_evaluator"]

[tool.hatch.build.targets.sdist]
include = [
    "/src",
    "/tests",
]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W"]
ignore = ["E501"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
